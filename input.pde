void mousePressed() {
    // if (startButton.inside()) startButton.press();
}

void mouseReleased() {
    launchPad.ignite();
    // if (startButton.pressed) {
    //     if (startButton.inside()) launchPad.ignite();

    //     startButton.release();
    // }
}
